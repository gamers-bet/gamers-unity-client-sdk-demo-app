# Unused project assets

`TextMesh Pro` contains the unused TMP Essentials materials and shaders retained
from the original demo project. The scene's required fonts, resources and shader
are now under the Reference Integration sample's `Text` folder. This directory
is outside `Assets` and is excluded from the distributed package.
